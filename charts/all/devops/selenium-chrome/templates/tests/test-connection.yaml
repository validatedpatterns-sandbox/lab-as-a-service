apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "selenium-chrome.fullname" . }}-test-connection"
  labels:
    {{- include "selenium-chrome.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "selenium-chrome.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
