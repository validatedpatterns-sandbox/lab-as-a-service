apiVersion: v1
kind: Service
metadata:
  name: {{ include "selenium-firefox.fullname" . }}
  labels:
    {{- include "selenium-firefox.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "selenium-firefox.selectorLabels" . | nindent 4 }}
