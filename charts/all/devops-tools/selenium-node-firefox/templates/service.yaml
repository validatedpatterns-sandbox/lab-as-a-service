apiVersion: v1
kind: Service
metadata:
  name: {{ include "selenium-node-firefox.name" . }}
  labels:
    {{- include "selenium-node-firefox.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  {{- range .Values.service.ports }}
    - name: {{ .name }}
      protocol: TCP
      port: {{ .port }}
      targetPort: {{ .port }}
  {{- end }} 
  selector:
    {{- include "selenium-node-firefox.selectorLabels" . | nindent 4 }}
