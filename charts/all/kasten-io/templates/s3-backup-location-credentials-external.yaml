---
apiVersion: "external-secrets.io/v1beta1"
kind: ExternalSecret
metadata:
  name: s3-backup-location-credentials
spec:
  refreshInterval: 15s
  secretStoreRef:
    name: {{ .Values.k10SecretStore.name }}
    kind: {{ .Values.k10SecretStore.kind }}
  target:
    name: s3-backup-location-credentials
    template:
      type: secrets.kanister.io/aws
  dataFrom:
  - extract:
      key: secret/data/global/kasten